"use strict";

// Populate this object with the account/character combinations you want to
// check. Each key is a string account name that maps to an array of strings
// representing character names. If you supply an empty array all characters
// will be listed.
exports.toCheck = {
};
