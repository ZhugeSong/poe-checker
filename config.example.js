"use strict";

// Configuration script for the main script.
// The main script requires a configuration script named "config.js" in the
// same directory; you can use this file as a base by copying it to
// "config.gs".

// Populate this object with the account/character combinations you want to
// check. Each key is a string account name that maps to an array of strings
// representing character names. If you supply an empty array all characters
// will be listed.
exports.toCheck = {
};
